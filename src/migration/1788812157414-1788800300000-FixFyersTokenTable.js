"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generated@86129 = void 0;
var default_1 = /** @class */ (function () {
    function default_1() {
    }
    return default_1;
}());
1788800300000;
FixFyersTokenTable1788812157414;
implements;
typeorm_1.MigrationInterface;
{
    name = '1788800300000FixFyersTokenTable1788812157414';
    async;
    up(queryRunner, typeorm_1.QueryRunner);
    Promise < void  > {
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `IDX_FYERS_TOKEN_AUTH_CODE_HASH` ON `fyers_tokens`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `IDX_FYERS_TOKEN_ISSUED_AT` ON `fyers_tokens`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `IDX_FYERS_TOKEN_STATUS` ON `fyers_tokens`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `IDX_FYERS_TOKEN_USER_ID` ON `fyers_tokens`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `idx_sio_provider` ON `side_income_opportunities`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `idx_sessions_expire` ON `sessions`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `uq_todo` ON `agent_todo_log`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `createdAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `updatedAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `side_income_opportunities` DROP COLUMN `created_at`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `side_income_opportunities` DROP COLUMN `updated_at`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` DROP COLUMN `todo_id`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` DROP COLUMN `updated_at`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `side_income_opportunities` ADD `createdAt` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `side_income_opportunities` ADD `updatedAt` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` ADD `todoId` varchar(32) NOT NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` ADD UNIQUE INDEX `IDX_e1f67cb010d5a1add75c061fff` (`todoId`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` ADD `updatedAt` timestamp(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `provider`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `provider` varchar(32) NOT NULL DEFAULT 'fyers'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `appId`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `appId` varchar(64) NOT NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `tokenDate`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `tokenDate` varchar(16) NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` CHANGE `accessTokenEncrypted` `accessTokenEncrypted` text NOT NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` CHANGE `refreshTokenEncrypted` `refreshTokenEncrypted` text NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `issuedAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `issuedAt` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `expiresAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `expiresAt` datetime NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `status`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `status` varchar(16) NOT NULL DEFAULT 'active'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `statusReason`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `statusReason` varchar(64) NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `userId`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `userId` varchar(64) NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `authCodeHash`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `authCodeHash` text NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `revokedAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `revokedAt` datetime NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` DROP COLUMN `broker`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` ADD `broker` varchar(32) NOT NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` DROP COLUMN `matchStatus`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` ADD `matchStatus` varchar(32) NOT NULL DEFAULT 'unmatched'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` CHANGE `importedAt` `importedAt` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `mail_accounts` CHANGE `isPrimary` `isPrimary` tinyint(1) NOT NULL DEFAULT '0'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `mail_accounts` CHANGE `active` `active` tinyint(1) NOT NULL DEFAULT '1'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `apply_settings` CHANGE `autoApplyEnabled` `autoApplyEnabled` tinyint(1) NOT NULL DEFAULT '0'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `applications` CHANGE `isSandbox` `isSandbox` tinyint(1) NOT NULL DEFAULT '0'"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `idx_tokens_issue_date` ON `fyers_tokens` (`issuedAt`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `idx_tokens_active` ON `fyers_tokens` (`status`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `idx_import_log_file_hash` ON `trade_book_import_log` (`sourceFilePath`, `fileHash`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `idx_trade_book_import_file` ON `trade_book_imports` (`importLogId`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `idx_trade_book_import_date` ON `trade_book_imports` (`entryTimestamp`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `idx_trade_book_import_source` ON `trade_book_imports` (`sourceFile`, `sourceRow`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `IDX_55d1900b9a21ced5d21b556a35` ON `side_income_opportunities` (`provider`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `IDX_e5d612f5400fecdea71f98ad6c` ON `sessions` (`expire`)")
    };
    async;
    down(queryRunner, typeorm_1.QueryRunner);
    Promise < void  > {
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `IDX_e5d612f5400fecdea71f98ad6c` ON `sessions`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `IDX_55d1900b9a21ced5d21b556a35` ON `side_income_opportunities`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `idx_trade_book_import_source` ON `trade_book_imports`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `idx_trade_book_import_date` ON `trade_book_imports`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `idx_trade_book_import_file` ON `trade_book_imports`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `idx_import_log_file_hash` ON `trade_book_import_log`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `idx_tokens_active` ON `fyers_tokens`"),
        await: await,
        queryRunner: queryRunner,
        : .query("DROP INDEX `idx_tokens_issue_date` ON `fyers_tokens`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `applications` CHANGE `isSandbox` `isSandbox` tinyint NOT NULL DEFAULT '0'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `apply_settings` CHANGE `autoApplyEnabled` `autoApplyEnabled` tinyint NOT NULL DEFAULT '0'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `mail_accounts` CHANGE `active` `active` tinyint NOT NULL DEFAULT '1'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `mail_accounts` CHANGE `isPrimary` `isPrimary` tinyint NOT NULL DEFAULT '0'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` CHANGE `importedAt` `importedAt` datetime(0) NOT NULL DEFAULT CURRENT_TIMESTAMP"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` DROP COLUMN `matchStatus`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` ADD `matchStatus` varchar(16) NOT NULL DEFAULT 'unmatched'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` DROP COLUMN `broker`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `trade_book_imports` ADD `broker` varchar(64) NOT NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `revokedAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `revokedAt` bigint NULL COMMENT 'Epoch timestamp when token was revoked'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `authCodeHash`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `authCodeHash` varchar(64) NULL COMMENT 'SHA-256 hash of auth_code for audit (never store plaintext)'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `userId`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `userId` varchar(50) NULL COMMENT 'FYERS user ID (FY ID) for audit'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `statusReason`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `statusReason` varchar(255) NULL COMMENT 'Reason for status change (e.g., replaced_by_new_tokens, user_revoked)'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `status`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `status` varchar(20) NOT NULL COMMENT 'active, revoked, expired' DEFAULT 'active'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `expiresAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `expiresAt` bigint NULL COMMENT 'Epoch timestamp when token expires (inferred from FYERS v3 doc if not provided)'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `issuedAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `issuedAt` bigint NULL COMMENT 'Epoch timestamp when token was issued (from FYERS response if available)'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` CHANGE `refreshTokenEncrypted` `refreshTokenEncrypted` text NULL COMMENT 'AES-256-CBC encrypted refresh token (nullable if not provided)'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` CHANGE `accessTokenEncrypted` `accessTokenEncrypted` text NOT NULL COMMENT 'AES-256-CBC encrypted access token'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `tokenDate`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `tokenDate` varchar(50) NULL COMMENT 'Date string from FYERS response (if available)'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `appId`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `appId` varchar(100) NOT NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` DROP COLUMN `provider`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `provider` varchar(50) NOT NULL DEFAULT 'fyers'"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` DROP COLUMN `updatedAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` DROP INDEX `IDX_e1f67cb010d5a1add75c061fff`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` DROP COLUMN `todoId`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `side_income_opportunities` DROP COLUMN `updatedAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `side_income_opportunities` DROP COLUMN `createdAt`"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` ADD `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `agent_todo_log` ADD `todo_id` varchar(32) COLLATE \"utf8mb4_0900_ai_ci\" NOT NULL"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `side_income_opportunities` ADD `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `side_income_opportunities` ADD `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `updatedAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3)"),
        await: await,
        queryRunner: queryRunner,
        : .query("ALTER TABLE `fyers_tokens` ADD `createdAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE UNIQUE INDEX `uq_todo` ON `agent_todo_log` (`todo_id`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `idx_sessions_expire` ON `sessions` (`expire`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `idx_sio_provider` ON `side_income_opportunities` (`provider`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `IDX_FYERS_TOKEN_USER_ID` ON `fyers_tokens` (`userId`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `IDX_FYERS_TOKEN_STATUS` ON `fyers_tokens` (`status`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `IDX_FYERS_TOKEN_ISSUED_AT` ON `fyers_tokens` (`issuedAt`)"),
        await: await,
        queryRunner: queryRunner,
        : .query("CREATE INDEX `IDX_FYERS_TOKEN_AUTH_CODE_HASH` ON `fyers_tokens` (`authCodeHash`)")
    };
}
